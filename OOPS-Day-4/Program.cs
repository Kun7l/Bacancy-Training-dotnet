namespace OOPS_Day_4
{
    class Program
    {
        //static void Main(string[] args) { 
        ////{
        ////    Order order = new Order();
        ////    order.Items.Add(new OrderItem("Book", 500, 1));
        ////    order.Items.Add(new OrderItem("Pen", 50, 2));

        ////    IPriceCalculator priceCalculator = new PriceCalculator();
        ////    IPaymentProcessor paymentProcessor = new UPIPaymentProcessor();
        ////    INotificationService notificationService = new EmailNotificationService();

        ////    OrderProcessor processor =
        ////        new OrderProcessor(priceCalculator, paymentProcessor, notificationService);

        ////    processor.Process(order);
        
        //}
    }
}